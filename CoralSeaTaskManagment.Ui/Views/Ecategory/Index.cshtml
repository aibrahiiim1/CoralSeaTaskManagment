@using CoralSeaTaskManagment.Ui.Models.DTO
@model IEnumerable<EcategoryDto>

@{
    ViewBag.PageTitle = "Category Management";
    ViewBag.CardTitle = "Category";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}
<a class="btn btn-inverse-primary btn-fw" href="/Ecategory/Create">Create</a>
<br />
<table class="table table-hover" width="100%">
    <tr>
        <td>Category</td>
        <td>Hotel</td>
        <td>Actions</td>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.Hotels.Name</td>
            <td>
                <a class="btn btn-outline-warning btn-fw" href="/Ecategory/Edit/@item.Id">Edit</a>
                <a class="btn btn-outline-danger btn-fw" href="/Ecategory/Delete/@item.Id">Delete</a>
            </td>
        </tr>
    }
</table>