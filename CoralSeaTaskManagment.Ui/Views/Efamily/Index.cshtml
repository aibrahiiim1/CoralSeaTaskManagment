@using CoralSeaTaskManagment.Ui.Models.DTO
@model IEnumerable<EfamilyDto>

@{
    ViewBag.PageTitle = "Family Management";
    ViewBag.CardTitle = "Family";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}
<a class="btn btn-inverse-primary btn-fw" href="/Efamily/Create">Create</a>
<br />
<table class="table table-hover" width="100%">
    <tr>
        <td>Family Name</td>
        <td>Hotel</td>
        <td>Actions</td>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.Hotels.Name</td>
            <td>
                <a class="btn btn-outline-warning btn-fw" href="/Efamily/Edit/@item.Id">Edit</a>
                <a class="btn btn-outline-danger btn-fw" href="/Efamily/Delete/@item.Id">Delete</a>
            </td>
        </tr>
    }
</table>